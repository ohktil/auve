// Ensure mintButton is defined and references the correct button element
const mintButton = document.getElementById('mintButton');

// Define a specific function to handle the click event
const handleMintButtonClick = () => {
  // Do something when the button is clicked
};

// Add the event listener with the arrow function
mintButton.addEventListener('click', handleMintButtonClick);
